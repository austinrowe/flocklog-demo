# Loaded by Vite when running in "demo" mode (npm run demo / npm run build:demo).
# Turns on the fake in-memory backend in src/demoBackend.js. No secrets here —
# this file is meant to be committed.
VITE_DEMO=1
